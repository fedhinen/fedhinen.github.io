---
import Layout from '../../layouts/Layout.astro';
import BlogList from '../../components/BlogList.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations('es');
---

<Layout title={`Victor Ruiz - ${t('nav.blog')}`}>
    <div class="mt-4 md:mt-12 mb-12 md:mb-24">
         <header class="mb-8 md:mb-16">
             <span class="badge-object mb-4 inline-block">{t('blog.object')}</span>
             <h1 class="text-3xl md:text-5xl font-[800] tracking-tight text-primary mb-4">{t('nav.blog')}</h1>
             <p class="text-gray-500 text-base md:text-lg max-w-2xl">{t('blog.gallery.description')}</p>
        </header>

        <BlogList lang="es" />
    </div>
</Layout>
